apiVersion: v1
kind: Service
metadata:
  name: {{ include "monitoring-agent.fullname" . }}
  labels:
    {{- include "monitoring-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monitoring-agent.selectorLabels" . | nindent 4 }}
